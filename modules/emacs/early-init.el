;; In early-init.el or init.el — applies to all frames including emacsclient
;; (add-to-list 'default-frame-alist '(alpha-background . 85))  ; 0–100

;; Better syntax highlighting 
(setq treesit-font-lock-level 4)
